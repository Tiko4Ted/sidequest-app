# Project Instructions

## graphify
- **graphify** (`~/.Codex/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
- When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

## SideQuest App Direction
- Build SideQuest UI work as a native Expo React Native app in `apps/mobile`.
- Treat the Vite web app in `src/` as an earlier reference prototype only.
- New screens, controls, and interaction work should be implemented natively first.
